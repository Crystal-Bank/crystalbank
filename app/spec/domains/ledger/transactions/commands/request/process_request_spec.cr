require "../../../../../spec_helper"

describe CrystalBank::Domains::Ledger::Transactions::Request::Commands::ProcessRequest do
  pending "requires an existing ledger transaction aggregate in the event store" do
    false.should eq(true)
  end
end
