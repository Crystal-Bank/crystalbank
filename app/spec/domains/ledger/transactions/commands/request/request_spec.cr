require "../../../../../spec_helper"

describe CrystalBank::Domains::Ledger::Transactions::Request::Commands::Request do
  pending "requires open accounts and a valid scope context" do
    false.should eq(true)
  end
end
