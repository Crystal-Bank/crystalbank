require "../../../../spec_helper"

describe CrystalBank::Domains::Ledger::Transactions::Api::Transactions do
  pending "POST /ledger/transactions - requires valid scope and open accounts" do
    false.should eq(true)
  end
end
