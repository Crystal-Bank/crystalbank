require "../../../../../spec_helper"

describe CrystalBank::Domains::Ledger::Transactions::Reactors::Request::OnRequested do
  pending "requires an existing ledger transaction aggregate in the event store" do
    false.should eq(true)
  end
end
